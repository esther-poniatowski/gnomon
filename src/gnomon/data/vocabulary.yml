# Generated by `gnomon vocabulary` from the filled records. Edit a record, not this file.
# A symbol is unique within its record, so the same letter may return under another record.
id: vocab.data
type: vocabulary
problem: data
generated_by: gnomon vocabulary
symbols: []
reused_across_records: {}
operators:
  canonical:
  - abs
  - adjoint
  - bools
  - card
  - complex
  - decreasing
  - depends_on
  - diff
  - dist
  - div
  - dot
  - drawn_from
  - empty
  - enum
  - ft
  - grad
  - implies
  - increasing
  - infinity
  - intersect
  - interval
  - ints
  - jacobian
  - lap
  - limit
  - maps
  - matrices
  - max
  - mean
  - measures
  - min
  - nonneg
  - norm
  - pos
  - prod
  - range
  - re
  - reals
  - spectrum
  - subset
  - sum
  - union
  - vectors
  declared:
    abs: the magnitude of a number or a vector
    adjoint: the adjoint of a map of a set to itself
    bools: the truth values
    card: the size of a set
    complex: the complex numbers
    decreasing: the monotone decrease of one quantity in another
    depends_on: the dependence of one quantity on another
    diff: the derivative of a quantity along the index given
    dist: the distribution formed over the indices it binds
    div: the divergence of a vector field over its domain
    dot: the inner product of two vectors
    drawn_from: the quantity is distributed as the law given
    empty: the set with no member
    enum: the set of the listed members
    ft: the Fourier transform over the indices it binds, introducing their conjugates
    grad: the gradient of a field over its domain
    implies: the implication between two statements
    increasing: the monotone increase of one quantity in another
    infinity: the unbounded limit of a quantity
    intersect: the members two sets share
    interval: an interval of an ordered set, or the interval between two bounds
    ints: the integers
    jacobian: the matrix of first derivatives of a map, at a point
    lap: the Laplacian of a field over its domain
    limit: the limit as the indices it binds approach their stated value
    maps: the maps from one set to another
    matrices: the arrays over a set, indexed by two sets
    max: the greatest value over the indices it binds
    mean: the mean over the indices it binds
    measures: the probability measures on a set
    min: the least value over the indices it binds
    nonneg: the nonnegative members of a set
    norm: the norm over the indices it binds
    pos: the strictly positive members of a set
    prod: the product of two sets
    range: the identifiers of a finite collection, given its size
    re: the real part of a complex number
    reals: the real numbers
    spectrum: the eigenvalues of a square array
    subset: the members of a set satisfying a predicate
    sum: the sum over the indices it binds
    union: the members of either set
    vectors: the tuples of a given length over a set
interactions: []
