---
tags:
  - handoff
index: "[Theoretical modeling tests](test/_index.md)"
aliases:
  - Framework correction handoff
---
# Handoff — framework corrections

The designer has ratified a reorientation that turns the inquiry records, formerly forms holding prose, into a small modeling language. In that language, each declared quantity carries a symbol, and every formula is an expression over these symbols. The records do not yet implement the reorientation completely, and the candidates that the reorientation does not absorb remain unreviewed.

## Reorientation

The records already depend on references between entries, yet the five cases showed these links to be unreliable. Within a record, for instance, `defined_over`, `applies_to` and `shares` resolve by exact equality of a prose name. Across records, `question.subject.domain` reaches a target parameter by the same equality. By contrast, `target_system` and `phenomenon` resolve by identifiers. Resolution by prose name is unreliable because a prose name carries a symbol, a gloss, a signature and sometimes a definition at once, so a formula can name a quantity that no entry declares.

Two ratified stages replace resolution by prose name. ^stages

| Stage | Content | Consequence for the records |
| --- | --- | --- |
| **A — symbols** | Every declared quantity carries a `symbol` unique within its record. `name` therefore holds a short label, and a prose field beside it states the content in words. References and formulas name quantities through their symbols. | One quantity per entry. References resolve mechanically. |
| **B — expressions** | A formula is an expression over declared symbols, with index variables bound by an aggregator. The free indices of an expression form the signature of the defined quantity. | An expression can aggregate over several indices. Grain is derived, not restated. |

The contract of both stages is owned by [the syntax, the binders and the resolution rule](../docs/epistemology/symbols-and-expressions.md). A stage is not complete while any dependent record still resolves a reference by prose.

A filled inquiry draws on three vocabularies, each with one author and one reach. ^three-vocabularies

| Vocabulary | Author | Reach | Where it lives |
| --- | --- | --- | --- |
| Fields and their option lists | Designer | Every problem | The method notes and the templates, kept aligned by the structural checks. |
| Symbols of one problem | Author of that problem | Its own records | The records themselves, collected into one generated table per problem. |
| Operators | Mixed | Canonical part everywhere, the rest in one problem | [The canonical set](../docs/epistemology/symbols-and-expressions.md#^operator-groups) sits in the registry. The remainder sits in one `operators.yml` per problem, read by all its records. |

## Task state

Seven theoretical inquiries each fill the three inquiry records, and six of them also carry a candidate answer and its assessment. Each proposed change rests on evidence recorded in [the diagnosis of the reported problems](framework-issues-synthesis.md), so each row below records only the implementation state of its change.

| State | Stage | Action | Approval gate |
| --- | --- | --- | --- |
| `done` | **Semantic owner** | Define symbols, expressions, binders and signatures in one note. Register it in the folder index. | One note owns the syntax. No other note restates the syntax. |
| `done` | **Templates** | Give every declared quantity a `symbol`. Replace the observable's index slots with `signature` and `definition`. Add `index_sets`, `manifestations`, and repeatable `scope_over_instances`. | Every template key has one responsibility. No key restates another. |
| `done` | **Method notes** | Align the notes on the target system, the phenomenon and the notation with the templates. | Every note row resolves to one template key in the same order. |
| `done` | **Resolution test** | Promote the reference checker into the test suite, with a negative case per rule. | The check fails on a planted unresolved reference. |
| `done` | **Fixture migration** | Symbolize all fifteen case records and restate their formulas as expressions. | All references in the fixtures resolve. No formula sits in a `name`. |
| `done` | **One source for the vocabulary** | Move the operator vocabulary into the note that owns the syntax, and point the resolution check at that note. | An operator removed from the note breaks every record in which it appears. |
| `done` | **Problem vocabulary** | Collect the symbols of each problem into a generated table. Split the operator set into a canonical part and a part that each problem declares for itself. | Every declaration reaches the table. No problem assumes an operator that it has not declared. |
| `done` | **Terminology and value sets** | Rename the law vocabulary, carry every formula under one field name, and write each value set and extent as an expression. | No field whose value is a set holds prose. Every expression field carries one name. |
| `done` | **Typed declarations** | Type the operators, and fix the object that an extent names. Declare the indices that an observable introduces, and give a domain restriction a kind and a set. | Every signature entry resolves. No interface variable names a set. No fact is stated by two fields. |
| `done` | **Keyword alignment** | Name every closed field after the distinction that it draws. Type operators with the keywords already used for quantities. Let an observable reference a quantity that the target declares. | No field name carries two meanings. No entry restates a declaration that another record holds. |
| `done` | **Exogenous variables and typed constraints** | Distinguish an exogenous variable, set outside the system, from a parameter held fixed. Tie each interface variable to its direction, and write the constraints of an external system as expressions. | No parameter runs along an interval. Each interface variable fits its direction. |
| `done` | **Implicit definitions and a derived boundary** | Let a quantity be fixed by a condition, and inherit operators along the record chain. Read the boundary from the shared variables, and retire the declared direction. | No observable is left undefined. No operator is declared twice in one problem. |
| `done` | **External systems characterized** | Give each external system a symbol. Let a parameter or an exogenous variable name the external system that it characterizes. | A quantity of an external system is declared and reachable, and its owner resolves. |
| `done` | **Detection tests, glosses and aim requirements** | Type the detection criterion, and fix which value a condition denotes. Admit a gloss beside an expression, and declare per aim the specifications that an inquiry cannot leave open. | Each detection test names the object that it checks and the set that it ranges over. No condition denotes an unspecified value. Each filled inquiry meets the requirements of its aim. |
| `done` | **Completeness by requirement and by aim** | Read from the notes the specifications that they require of every inquiry, and let an aim relax any one. State the accuracy exclusion as a consistency between fields. | Every record carries the specifications that the notes always require, unless its aim leaves one open. |
| `done` | **Registries as sources** | Move every vocabulary and requirement into package data, and leave the notes to document the declarations of the registries. | No module reads a document. A note that drifts from its registry is reported. |
| `done` | **Closed vocabularies as a registry** | Move every closed vocabulary into package data, and check each filled value and each template comment against the registry. | A value outside its vocabulary is reported. A template comment that drifts is reported. |
| `done` | **Contract of the answer layer** | Realign the satisfaction tests with the fields that the question and phenomenon records now carry, and sweep the notes for structures that the reorientation retired. | No test names a field that no longer exists. No note references the retired interface. |
| `done` | **Sources of variation and the quoting convention** | Let a source of variation name the declared quantities that it varies, and settle when a value carries quotation marks. | Every source of variation resolves. An expression that a serializer split into fragments is reported. |
| `done` | **Notation, and the scale retired** | Give each symbol and operator its LaTeX form, and retire the scale that a value set already fixes. | A renderer composes LaTeX without parsing an expression. No field states a grain that the value set carries. |
| `done` | **Statements, conditions and foils** | Read the statement of the phenomenon as a gloss. Split the statement of the question into an authored gloss and a generated rendering, and type the precipitating conditions and the foils. | No prose field states a claim that a formula could carry. A condition of the phenomenon resolves. |
| `done` | **Event protocol and proof obligations** | Let an event observable declare its horizon and censoring. Let a question list the results that an answer must derive, separately from the outputs that it must match. | A censored event is distinguished from a value beyond the horizon. Each implication receives its own verdict. |
| `done` | **Matched targets and admitted parts** | Identify each output that an answer must reproduce by its target, and name the admitted parts by the sets that they belong to. | No accuracy entry restates its own target. The compositional level resolves. |
| `done` | **Field order** | Put the language before the entries written in it, and let each entry open on its identity and follow with its construction. | A reader meets each declaration before its use, and each gloss follows its formula. |
| `done` | **Names first and glosses on the cases** | Open each entry with its name, and render every formal expression of the seven cases in words. | A reader meets the name before the symbol, and every expression arrives with a gloss. |
| `done` | **Named entries and one operator file** | Give every entry a name distinct from its gloss, and collect the operators of a problem into one file that its records share. | An entry states its name, its written form and its meaning, each once. No record declares an operator. |
| `done` | **Justified requirements** | Let each matched output and structure, and each domain restriction, state why it is demanded. | A reader of a question record sees the outputs that the answer must reproduce and the reason for each demand. |
| `done` | **Level reduced to its two axes** | Retire the aggregation token, and reduce the level to the two axes that both notes describe. | No field of the question restates a construction that a declaration carries. |
| `done` | **The question layer is read** | Load the question records into the resolution checks, and report a declared quantity that no entry uses. | Each expression resolves in all record kinds, and a declaration with no consumer is named. |
| `done` | **Named alternatives and justified virtues** | Give a name to all foils and proof obligations, and let a selected virtue carry the reason why it is weighed. | In a question record, no entry states only a gloss, and every choice carries a reason. |
| `done` | **Selection criteria typed** | Carry each criterion of the reference class as an expression that its members satisfy, with the reason attached. | No field of the target system holds a criterion in prose. |
| `done` | **Conditions and byproducts typed** | Carry each of the five standard kinds as an expression with a name and a reason, and fill the entries that the cases can now state. | No kind of condition is held in prose, and every filled entry resolves. |
| `done` | **Boundaries decided** | Let every external system list its constraints or state that it imposes none. Declare the quantities that those constraints needed. | No external system leaves its constraint open, and every declared diagnostic is used. |
| `done` | **Internal interactions** | State a directed influence between two parts as its own entry, and generate the interaction graph from the record. The `organization` block later superseded this entry. | Every edge names two declared endpoints and a quantity indexed by both, and `gnomon vocabulary` prints the graph. |
| `done` | **Target variants** | Let one reference class hold members that differ in their declarations. Report a law that ranges over the whole class yet uses a symbol that only some members carry. | Every declaration states which members carry it, and no unmarked law names a symbol of one variant alone. |
| `done` | **Blocks reduced to their distinctions** | Retire the interaction block, and move each of its entries onto the asserting law. Merge the two variable blocks, and return a restriction on prior values to the question. | No block encodes a distinction that a field elsewhere already carries. |
| `done` | **The law entry reduced to its claims** | Define the range of a law, type the laws by the kind of claim that they assert, and carry an influence as the order of its pairs. The `organization` block later retired `influence`. | No law field restates another, and the role of a law identifies the kind of claim that the law makes. |
| `done` | **One word, one key, any arity** | Give the places of a law the word that every other entry uses for them, carry each influence in one key, and admit arities greater than two. The `organization` block later retired `influence`. | A law and a quantity state their places under one word, and a joint influence is expressible. |
| `done` | **Restrictions in one block** | Merge the three restriction blocks, and check the rules that their separation carried implicitly. | A restriction states which quantity it holds and in which manner. The checks state the rules that the block boundary formerly implied. |
| `done` | **A phenomenon holds of the members that bear it** | Let each entry of a phenomenon name the variant that it holds of. Report an entry that ranges beyond the members carrying its symbols. | Each entry of a phenomenon is readable by all members that it ranges over. |
| `done` | **An empty variant is reported** | Count the declarations carrying each variant. Let a maturity marker admit a variant that the record has not written. | A variant with no schema is either carried or declared open. |
| `done` | **The operator vocabulary typed and widened** | Make an operator generic over the sets that it acts on. Elevate two groups of operators: those that the framework's own fields require, and those that no problem owns. Name the shapes that a reader meets, and check each call against the type of its callee. | An operator is typed once, independently of its call sites, and `open` in a type means unknown, never generic. |
| `done` | **The derivative names its index** | Retire the operator that hardcoded a course, and let a derivative run along any index that a quantity takes. | A record that differentiates along its own course names that index, and a second derivative is the first taken twice. |
| `done` | **Six debt items settled** | Give the link a field, retire the unused requirement form, and let a relation run between laws. Give the maturity marker a consumer, let a detection test name its bound, and finish two rename sweeps. | Each affordance that the notes promised is expressible and checked, and no note names a field that does not exist. |
| `done` | **A virtue states its standing, and understanding is not an aim** | Let each virtue state whether it is constitutive of the product or a preference among admissible answers. Retire the aim whose whole content was two virtues. | A question states the cost of failing a virtue. An inquiry can demand understanding under any aim that it pursues. |
| `done` | **An answer names the members that it accounts for** | Let a record name the target variant that it is about. Check every mark against the declarations of variants, and extend the overreaching rule to the blocks that carry one. | A mark names a variant that exists, and no entry of any block asserts of the whole class a property that only some members can bear. |
| `done` | **The two levels named apart** | Name the block that limits an answer after the bound that it sets, and name the accuracy field after the demand that it makes. | No two fields of one record carry the same name for unrelated objects. |
| `done` | **External systems that an answer draws on** | Let an explanans name the external system that it appeals to. Make each such system a symbol that the rest of an inquiry can reach. | An appeal resolves to a *declared* external system. |
| `done` | **External systems stated as a class and as claims** | Name the class that each external system belongs to. Give each constraint that it imposes the form of a law. | An external system states its class, and each constraint that it imposes is written as a claim. |
| `done` | **The answer declares a model** | Let a candidate declare its model with the blocks that a target system uses, and give every law a warrant. State the premises that the explanans relies on. | No block of the answer draws a distinction differently from the target. No field of the answer is unfillable when a question states no contrast. |
| `done` | **Organization named, and the chain from a factor stated** | Give a law a symbol, state the relations among the parts and the laws as the organization, and let a factor specify how it reaches the phenomenon. | A relation is an entry of its own and is realized by laws that each carry a name. An explanans can be traced from its factor to the explanandum. |
| `done` | **Prose split by the job that it does** | Name each prose field after the job that its own placeholder states, and document the five jobs. | A reader of a field knows whether it translates, justifies, distinguishes, describes a procedure or records a gap. |
| `done` | **A factor may be a property that no record states** | Let a factor bear on several elements. State the bearer that each kind of factor names, and admit a property that the answer has found but not formalized. | An explanans can carry the findings of the analysis, beyond the declarations that the records already hold. |
| `done` | **The assessment brought up to the records** | Repair the criteria that named retired fields, and let each test name the objects that it now compares. Hold a candidate to the warrant that its laws claim. | No criterion names a field that no longer exists, and a candidate can fail the warrant that a law carries. |
| `pending` | **Remaining candidates** | Review the candidates that the reorientation does not absorb. | Each has one review state and one owner. |
| `pending` | **Trial of the answer layer** | Instantiate and assess two candidates: one predictive, one for a theorem task. | Both assessments use the question contract without adding or reinterpreting any of its fields. |

## Classification record

Each candidate carries one review state, assigned by the designer. A candidate marked `absorbed` needs no separate implementation: the reorientation already supplies its capability, and the acceptance test named in the diagnosis is met.

| Review state | Meaning |
| --- | --- |
| `absorbed` | The reorientation supplies the capability. Implemented. |
| `approved` | Implemented as its own change. |
| `pending` | Not yet reviewed. |
| `deferred` | Recorded, outside the current scope. |
| `rejected` | Recorded as excluded. |

| Candidate | Owner | Review state | Disposition |
| --- | --- | --- | --- |
| Symbols across fields and records | Shared schema | `approved` | Stage A. |
| Expressions with bound indices | Shared schema | `approved` | Stage B. |
| Observable construction | Shared schema | `absorbed` | A derived observable carries a `definition`, and the indices that remain free form its `signature`. |
| Instance scope and uncertainty | Shared schema | `approved` | `scope_over_instances` is repeatable, and each entry names its source of variation, its statistical role and its conditioning. |
| Varied factors with unknown effects | Shared schema | `absorbed` | A varied factor indexes the observable when the claim is a function of its values. It becomes a scope entry when the claim states a distribution over the draws of the factor. Neither role asserts an effect. |
| Mathematical domains treated as constituents | Shared schema | `approved` | An index set enters `index_sets` only when neither a constituent kind nor a constituent parameter supplies the set. |
| Phenomena that relate several variables | Shared schema | `approved` | `manifestations` carries the form that the observables take. |
| Internal relations typed as external interfaces | Shared schema | `approved` | `external_systems` fixes the boundary of the system, so a relation with both endpoints inside it had no place. The `internal_interactions` block first supplied that place, naming the endpoints, the ordered pairs, and the declared quantities of each interaction. The `organization` block now holds these relations and retires `internal_interactions`. Three cases carry at least one internal interaction: a transmitted signal between climate subsystems, a nonreciprocal coupling between species fields, and three edges among populations and resource pools. |
| Input–output role of a law | Shared schema | `approved` | The role vocabulary gained the value `input-output`. The record on delayed generalization now gives its forward map that role, so no comment marks the gap. |
| Target variants | Shared schema | `approved` | The declarations of each target stay in a single table, because the resolver and every reference across records assume that layout. Within that table, the declarations are partitioned: an entry that only some members carry is marked with its variant. For example, the microbial class now separates a community that models its resources explicitly (`resource_explicit`) from a community whose species interact directly (`direct`). Fourteen of its symbols belong to one variant. Its law of population change is split into two laws, and the growth law takes different arguments in each. |
| Task obligations and accuracy | Shared schema | `approved` | `proof_obligations` states each implication as an ordered antecedent and consequent, and the assessment carries one verdict per obligation. The proof of the microbial inquiry no longer occupies the fidelity field, so the accuracy of that field is now `not-applicable`. |
| Scoped virtues | Shared schema | `pending` | Reduced: a virtue can now name a symbol and a scope entry, both declared. |
| Provenance | Shared schema | `pending` | Not addressed. |
| Generated table of symbols, one per problem | Shared schema | `approved` | `gnomon vocabulary` collects every declaration of a problem from its own records, so an author consults one table spanning the records that they fill. |
| Single table of symbols across problems | Shared schema | `rejected` | A symbol is unique within its record, so the corpus as a whole fixes no set of free letters. The same letter also denotes a separate quantity in each of several problems. |
| Canonical and local operators | Shared schema | `approved` | The framework supplies the operators that its own semantics require. Each problem declares the operators of the mathematics that it brings. |
| Operator vocabulary declared once | Shared schema | `approved` | Each operator is declared in [the groups of the expression syntax](../docs/epistemology/symbols-and-expressions.md#^operator-groups), and the resolution check reads that source. |
| Law vocabulary renamed | Shared schema | `approved` | Three terms were renamed: `function` to `role`, `succession` to `evolution`, and `coexistence` to `constraint`, the term that the records already used in their labels. |
| One name for every expression | Shared schema | `approved` | `statement` and `definition` became `expression`. The block holding the expression fixes whether it introduces a quantity or asserts a falsifiable claim. |
| Value sets and extents as expressions | Shared schema | `approved` | `values` and `extent` carry set expressions built from canonical constructors. `constituents.number` became `extent`, the concept that an index set already carried. |
| Operators carry a type | Shared schema | `approved` | Each declared operator states its `kind`, its argument sets (`takes`) and its value set (`gives`). |
| Extent identifies members | Shared schema | `approved` | A constituent kind collects identified units, so its extent is `range(S)` or `enum(...)`, never a value range. The count of those units carries a symbol of its own, and a bound on it is a constraint. |
| Observables declare their own indices | Shared schema | `approved` | The phenomenon record gained `index_sets`, so a separation, a lag or a horizon is declared. A signature entry that resolves to nothing is now reported. |
| Interface variables are quantities | Shared schema | `approved` | An interface variable resolves to a declared state variable or parameter, never to a constituent kind or an index set. |
| Domain restrictions are typed | Shared schema | `approved` | A state restriction carries a kind from a closed list and the set it is held to. |
| Vacuous `shared` removed | Shared schema | `approved` | `shared` asserts one value across a signature, so an entry that takes no index omits the key. |
| Vocabulary stored as a record | Shared schema | `approved` | `gnomon vocabulary` writes `vocabulary.yml`, and its `--markdown` option builds the table beside the record. |
| Operators are typed with the quantity keywords | Shared schema | `approved` | `takes` and `gives` became `signature` and `values`, since a quantity indexed by a set and an operator taking an argument in it have the same form. |
| Generic field names retired | Shared schema | `approved` | `kind` carried seven vocabularies and `role` three. Each vocabulary now carries the name of its distinction: `structure`, `basis`, `restriction`, `quantification`. |
| Operator category derived | Shared schema | `approved` | A binder is written with bound indices and a relation gives `bools`, so the label restated the syntax and was removed. |
| Observables reference declared quantities | Shared schema | `approved` | An explanandum variable carries a *`reference`* when the target already declares the quantity. |
| Exogenous variables declared | Shared schema | `approved` | A variable that is set outside the system and varies while the phenomenon unfolds is neither internal nor held fixed. For lack of that category, five quantities were misclassified as parameters or state variables. |
| Interface tied to direction | Shared schema | `approved` | An output or a coupling is a state variable. An input is an exogenous variable or a parameter. |
| External constraints typed | Shared schema | `approved` | `constrains` holds *expressions*, so a constraint that restates a declared value set becomes visible. |
| The selection `basis` renamed `selects_on` | Shared schema | `approved` | `basis` reads as a basis of a vector space, so the field became `selects_on`, with values `prior-property` and `outcome`. |
| Quantities fixed by a condition | Shared schema | `approved` | A `condition` replaces an `expression` where no map builds the quantity, and it marks the presupposition that a value satisfying it exists. |
| Operators inherited along the record chain | Shared schema | `approved` | A phenomenon reaches its target's operators, so declaring one twice within a problem is a defect. |
| Boundary derived from the shared variables | Shared schema | `approved` | The `interface` block and its `direction` restated the boundary that the declarations already fixed. The derivation follows the behavioral approach, in which an interconnection is a set of shared variables with no prior partition into inputs and outputs [willems2007]. |
| Answer declarations reference the target | Shared schema | `approved` | A constituent or a term either references a quantity that the target declares or carries its own expression. Both forms replace a `kind` field whose value `declared` identified no referent. |
| External systems carry quantities | Shared schema | `approved` | [A domain restriction](../docs/epistemology/domain.md) could already name a parameter of an external system, but the target record gave that quantity no slot. A quantity now names its owner, and the record keeps one symbol table. |
| Section order kept | Shared schema | `rejected` | The record orders its blocks from sets through quantities and relations to the environment, and each references only its predecessors. External systems placed beside the constituents would therefore precede the variables that they share, and so break that order. |
| Detection criterion typed | Shared schema | `approved` | The criterion became a list of tests, each stating its requirement and the sources of variation that it must hold across. Before the change, one case had restated its manifestation in the criterion as prose. |
| Conditions state which value they denote | Shared schema | `approved` | `selects` fixes the reading over the values meeting the condition: `unique` presupposes a single value, `any` holds of each, and `all` makes the quantity their set. |
| Expressions admit a gloss | Shared schema | `approved` | An optional gloss renders an expression in words and carries no commitment that the typed fields could hold. |
| Requirements declared per aim | Shared schema | `approved` | [The fields that the inquiry of each aim cannot leave open](../docs/epistemology/epistemic-aim.md#^required-by-aim) are declared in one place, and the check reads them there. |
| General requirements read from the notes | Shared schema | `approved` | The "When it is required" column of every specification table already stated the general requirements. The rows marked `always` are now enforced by [the completeness check](../src/gnomon/completeness.py). |
| An aim may relax a general requirement | Shared schema | `approved` | An aim that produces a phenomenon cannot be asked to state it in advance, so the aim table carries a third column naming the fields that it admits open. |
| Accuracy excluded by a consistency rule | Shared schema | `approved` | No aim, target type or requested relation decides whether an answer is matched against a measurement, so the rule is a consistency between fields. `not-applicable` joins the accuracy vocabulary. |
| Vocabularies and requirements as package data | Shared schema | `approved` | The sources are package data: [the operators](../src/gnomon/data/operators.yml) and [the requirements](../src/gnomon/data/requirements.yml). The checkers parse *YAML*, and the package no longer needs a workspace holding `docs/`. |
| Documentation checked against the registries | Shared schema | `approved` | Only the alignment check reads a document, so it compares the operator bullet, the aim table and every row marked `always` against the registries. |
| Closed vocabularies as a registry | Shared schema | `approved` | [The vocabulary registry](../src/gnomon/data/vocabularies.yml) declares all 37 closed vocabularies, keyed by the block enclosing each field. A filled value and a template comment are both checked against the registry. |
| Satisfaction tests realigned | Shared schema | `approved` | The grain test named `aggregation` and `profile`, two fields retired by earlier stages. The grain test now names the signature and the expression. The individuation, level, scope and product tests now name the replacements of the fields that they described. |
| Sources of variation made operational | Shared schema | `approved` | `over` names the declared quantities that a source varies, and the prose moves to a gloss. |
| Quoting settled | Shared schema | `approved` | A compound value is quoted and a bare identifier is not. Inside a bracketed list the quotes are required, since an unquoted expression splits on its commas into fragments that each read as a valid value. |
| Notation for rendering | Shared schema | `approved` | A symbol carries its printed form. An operator carries a template with `#0` for the bound indices and `#1` for each argument. |
| Scale removed | Shared schema | `approved` | The vocabulary was built for a scalar and was applied to ten observables valued in a matrix, a measure or a function space. The value set, moreover, already fixes which comparisons are meaningful. |
| Domain moved beside the selection criteria | Shared schema | `pending` | Proposed: the domain restricts the same specification that the criteria select on, so both belong to the target. |
| Contrast moved into the phenomenon | Shared schema | `pending` | Proposed: the foils are values of an explanandum variable that the phenomenon declares. |
| Bearer and behavior read as a gloss | Shared schema | `approved` | The signatures fix the bearer and the manifestations fix the behavior, so the statement renders both in words and carries no commitment of its own. |
| Question statement split in two | Shared schema | `approved` | An authored gloss opens the record. The renderer composes the second statement from the fields. |
| Conditions and foils typed | Shared schema | `approved` | A precipitating condition and a foil are expressions with a gloss. Five of seven foils and both conditions were expressible once the dissipation scale was declared. |
| Event protocol | Shared schema | `approved` | An event observable carries its origin, its horizon and its censoring. A run that never reaches the criterion is therefore recorded as *censored*. |
| Matches name only their target | Shared schema | `approved` | `model_output` and `model_structure` restated the target side: an answer reproduces the observable named opposite. Each entry now carries the target and the tolerance. |
| Manifestations carry a symbol | Shared schema | `approved` | An inquiry may require an answer to reproduce a *manifestation*, as the case on delayed generalization does for the ordering of its two crossing times. |
| Compositional level made a reference | Shared schema | `approved` | The compositional level names the constituent kinds or index sets whose parts may enter the explanans. The level is empty where an answer explains by laws of the whole system. |
| Field order settled | Shared schema | `approved` | `operators` precedes the declarations whose formulas use those operators. An entry runs identity, type, construction, then gloss. The answer carries an authored statement near the top, as the question does. |
| Names precede symbols | Shared schema | `approved` | A reader identifies an entry by its name, so `name` and `label` open it and `symbol` and `notation` follow. |
| Glosses across the cases | Shared schema | `approved` | Forty-five expressions carried no rendering in words. Every formal field of the seven cases now carries one, 101 in all. |
| Definitions that restated a law | Shared schema | `approved` | Three state variables carried an expression that a law of the same record already stated. The law owns the claim, so the expression is null. |
| `label` renamed `name` in law entries | Shared schema | `approved` | `label` alone among entry keys differed from the `name` that every other block uses, so 23 law entries and the template now use `name`. |
| Manifestations, scope entries and tests named | Shared schema | `approved` | Thirty-two entries carried a symbol and a gloss and no name. Thirteen of those glosses were noun phrases that served as names. Each of the thirteen became the name of its entry and left the gloss field empty. |
| A gloss adds content that the name leaves unsaid | Shared schema | `approved` | A check derived from the *rule* found two glosses whose content words already appeared in the name. Both now state content that the name and the typed fields leave unsaid. |
| One operator file per problem | Shared schema | `approved` | The thirty-one operators of the seven problems were declared in eleven blocks across two record kinds. Each problem now has [one operator file](../src/gnomon/data/templates/operators.yml). Each record of the problem resolves its calls against those operators. Their signatures may use any set that the problem declares. |
| External systems glossed | Shared schema | `approved` | The shared variables name the quantities crossing the boundary, whereas the gloss states the quantities or conditions that the external system supplies or imposes. |
| A feature fixed by a condition | Shared schema | `approved` | An `observed` value held a constraint in prose. A feature is a quantity, so it takes the `condition` and `selects` keys. The check that a condition names its selection rule now covers these features too. |
| Requirements and restrictions justified | Shared schema | `approved` | Twelve entries across the domain and the required accuracy now carry a gloss. A restriction lists the values that it removes from the domain, but no typed field records why the question makes that choice. The gloss therefore carries the reason. |
| The limit is an expression with a gloss | Shared schema | `approved` | One case held two branches of prose in the field. The field now carries an expression and the reason why the question concerns the limit system. The second branch is named as a domain that the record does not carry. |
| Aggregation retired | Shared schema | `approved` | The field was retired for three defects. First, one token per question could not carry a property of each variable. Second, its four values were not a partition, and they named fourteen of the twenty-three aggregating binders in use. Third, the binder of the declared expression already wrote the construction. In addition, both fills described objects that the field does not name: an ensemble over instances, owned by `quantification`, and an average over index sets in a target declaring no constituents. |
| Question records resolved | Shared schema | `approved` | `DECLARING` had listed two sets: the blocks in which a kind declares symbols, and the kinds that the resolver loads. A kind without symbols of its own was therefore never read. `RECORD_KINDS` now names the kinds that are read. `PATH_FORMULA` reaches the expressions that a question nests too deeply for a block and a key to address: 21 of them, over eight paths, each verified to resolve to content. A question resolves against its whole problem, as an operator file does. |
| Unused declarations reported | Shared schema | `approved` | The check reported an operator that no record used, but not an unused quantity. Six parameters were declared and never reached. Each now enters the record meant to use the parameter. |
| `infinity` declared | Shared schema | `approved` | Three domain limits used a bare word that no record defined. The unbounded limit is framework vocabulary, not problem mathematics, so it joins the canonical registry beside the nullary set constructors. |
| Foils and obligations named | Shared schema | `approved` | Seven foils and two proof obligations carried a gloss and no name. Foils and proof obligations were the last two blocks outside the naming rule. |
| Virtues carry their reason | Shared schema | `approved` | No field recorded why an inquiry weighs a virtue, because each was a bare token. Each is now an entry with its gloss. The change moved the vocabulary key from `.virtues` to `virtues.virtue` and the aim requirement to `question.virtues[].virtue`, and it gave `at` a step that traverses a list. |
| Selection criteria typed | Shared schema | `approved` | `criterion` held a sentence. The field became `expression`, the name that every formula carries. For example, in the class on delayed generalization, the criterion of overparameterization reads `P > card(train)`. The reason why membership is restricted moved to a gloss, and the resolver now reads the field. |
| Conditions and byproducts typed | Shared schema | `approved` | The five kinds form a closed list [craver2013]. Three of them held prose: a modulating condition stated its effect in words, and a nonstandard circumstance and a byproduct were bare strings. Each now carries a name, an expression and a gloss. A modulating condition also carries the relation that it induces, written with operators that the canonical set already holds. The resolver reads all four new fields. |
| External boundaries decided | Shared schema | `approved` | Three of six constraints read `open`, a value indistinguishable from imposing nothing. Two were resolved by declaring the diagnostics of the prescribed trajectory, and one by writing the empty list that its own comment already described. [The completeness check](../src/gnomon/completeness.py) reports an external system that leaves the field open. |
| An exogenous variable may be derived | Shared schema | `approved` | A rate built from a prescribed trajectory had no place in the record, because the exogenous block carried no `expression`. Declared instead as a parameter, the rate was reported by the check that no parameter runs along an interval. The exogenous block now carries an expression and a gloss, as the state variables do. |
| Interaction carried by its law | Shared schema | `approved` | Two of five interactions restated a law that already named their carrier. Three had no law only because the record did not write one: each is now `depends_on(F, A)` or a law of the same form. The `interaction` sub-block marks the law that states an influence, just as `event` identifies an observable. The carriers are read from the expression of that law. |
| Variables merged | Shared schema | `approved` | The three quantity blocks shared every key and no key belonged to one alone, so two of them encoded one distinction. `variables` carries `determined: within \| outside`. `parameters` stays separate: a parameter may not be indexed by the course, and a rule enforces that boundary. |
| Selection narrowed to an outcome | Shared schema | `approved` | One target serves several phenomena, so a criterion written there binds every inquiry. That binding is correct for a population selected on an outcome, because every inquiry must account for that criterion of admission. The same binding is incorrect for a restriction on prior values. The domain therefore gained a joint restriction for a condition that no single quantity carries, and `selects_on` was retired with its second value. |
| `applies_to` defined | Shared schema | `approved` | `applies_to` lists the places over which the claim ranges, as a signature does for a quantity. The six interaction laws were filled to that definition. The field stays declared, as a signature is, because a mechanical derivation collects both too many indices and too few. Derived mechanically, the field would take the index of a quantity that an operator consumes and would miss a quantity applied at a literal index. |
| Interaction reduced to its pairs | Shared schema | `approved` | `acts_on` was an element of `applies_to` in all six interactions, and `acts_from` and `acts_on` agreed with the order of `pairs` in all six. The ordered pairs carry the direction, so the block is one key, and a pair set that names no product of two declared kinds is reported. |
| Law roles rebuilt | Shared schema | `approved` | `constraint` held 17 of 29 laws. Six values derived from the claims of the 29 laws leave the largest class at 9 laws: `evolution`, `state-constraint`, `admissibility`, `distribution`, `input-output`, `measurement`. A law computing an output from a state with no derivative was typed `evolution` and is now `input-output`. |
| Granularity retired | Shared schema | `approved` | Granularity split the laws into 20 collective and 9 local. No check, no rule in a note and no downstream field read the split. Among the law fields, granularity alone had no consumer. |
| `applies_to` renamed `signature` | Shared schema | `approved` | The places that a claim ranges over and the indices that a quantity still takes are one notion. The framework already called that notion a signature. |
| The influence is one key of any arity | Shared schema | `approved` | The sub-block held a single key, so it was flattened onto the law. Its product was binary, although this reorientation began by removing fixed arities. The reach is now the last place and every source precedes it, so the microbial higher-order term states that two populations change the growth of a third together. |
| Restrictions merged | Shared schema | `approved` | The named quantity already distinguished a parameter from a state. Likewise, the absence of a named quantity already marked a joint restriction. The three blocks therefore carried no distinction of their own, and no check read the split. One block now holds all three. [The rules that the split carried](../src/gnomon/completeness.py) are stated explicitly. First, a restriction does exactly one of two things: it fixes one quantity to a set, or it carries the expression under which it holds. Second, an entry naming no quantity carries a name. Third, only an entry that names a variable states how that quantity is held, since a parameter is already fixed. |
| The law's expression and its influence distinguished | Shared schema | `approved` | Only the expression of a law can be false of the system. The influence asserts nothing and names the tuples along which the expression runs. The template states the distinction beside each key. |
| Phenomenon entries marked by variant | Shared schema | `approved` | `variant` reached the target alone, so a condition true of one kind of member and unreadable for another had to stay in a comment. Every entry of a phenomenon now names the variant that it holds of, and a check across records reads which members carry a symbol from the target. Six entries ranged beyond their bearers. For example, the whole active-matter phenomenon is stated over its continuum realization, and the microbial supply varies a quantity that a member with direct interactions (`direct`) does not have. |
| The exclusion bound written | Local case | `approved` | `S > M` inhibits coexistence in a consumer-resource community and cannot be read of a community whose interactions are direct, so the record states the bound for `resource_explicit` alone. |
| Unborne variants reported | Shared schema | `approved` | The active-matter lattice realization carries no declaration, so the class announces a disjunction that it satisfies on one side. The count of declarations per variant is structural, because a lexical count finds the name five more times: in its own gloss, in an unrelated parameter and in the marker admitting the realization. The marker is now *read*, so `maturity` has its first consumer. |
| The link is a mapping, not a third description | Shared schema | `approved` | `substrate: link` obliged an answer to supply both descriptions and a mapping between them, although no field held one. Both descriptions are sets of quantities and laws, and the answer's ordinary blocks already declare such sets. Only the mapping was new, so it is one block and not a second model. `unrealized_link` reports a question asking for a link whose answer maps nothing. |
| A requirement is a path, never a value | Shared schema | `approved` | The registry could require a field to list a value, and only the retired understanding aim used that form. An aim fixes the content that an answer must deliver, not the value that a field takes. A rule reaching inside a field is therefore a consistency between fields, as `contradictory_accuracy` is. The form is removed from the registry, the checker and its synthetic test. |
| A relation of the organization holds between parts or between laws | Shared schema | `approved` | The note documents relations among the parts *or the laws*. `relates` could name only a product of declared kinds. The schema therefore expressed less than the documented language and the mechanism ontology that the framework adopts. `among` names the laws in order, with no carrier, since no quantity is indexed by a law. No case states such a relation, and the reason is now recorded: where the law expressions share an index that fixes the order, a relation would restate the laws, and only an order that no index carries would warrant such a relation. |
| An unbounded explanans states why its bound is empty | Shared schema | `approved` | Four questions left the bound empty, three as a finished decision and one as work not done, and all four read alike. No case used the maturity marker, although it existed for this distinction. Each decision now carries a justification. The bound in the grokking case carries the marker, because its own expressivity challenge states that the level must not be fixed prematurely. `maturity` thereby gains its second consumer. |
| A detection test names the instruments that limit it | Shared schema | `approved` | The record on tipping cascades declared its instruments and proxies as an external system. A comment stated that their coverage and uncertainty bear on the detection criterion rather than on the target, since no field could hold that statement. `limited_by` on a detection test names the instruments and proxies, and the comment no longer restates the bound that the phenomenon now carries. |
| `understanding` retired as an epistemic aim | Shared schema | `approved` | Every other aim adds a requirement on the subject or the task. This aim, by contrast, added two entries in the appraisal block and nothing else. The note on virtues therefore had to exempt this aim from its own defining claim that a virtue is not constitutive of the task. Two further measurements settled the retirement. First, a single record named the aim, and it also filled every field that `explanation`, `modal-knowledge` and `prediction` require. The aim therefore contributed the virtues alone. Second, stripping that record of its foil set and its requested relation left it reported complete, because naming the aim relaxed the checks that its own content needed. Understanding is also sought *of* a product that another aim delivers, and a single-valued field cannot hold both aims. |
| Each virtue states its standing | Shared schema | `approved` | The table stating the requirements of each aim was the sole carrier of the distinction between constitutive and preferred virtues. With the aim retired, intelligibility and structural transparency would therefore have dropped to appraisal, where failing them does not make an answer inadmissible. The key is also warranted on its own evidence: all ten virtues that the corpus fills justify themselves as conditions of answering at all, yet the schema treated eight of them as preferences. A count by regular expression found five such virtues, and a reading of all ten identified ten. The reported count therefore rests on the reading. |
| An answer names the variant that it accounts for | Shared schema | `approved` | An answer resting on a quantity that only some members declare accounts for that subset, not for the class, and the record had no key stating that restriction. The record now carries a `variant`, its entries carry one each, and an unmarked entry inherits the variant of the answer. The laws of a target already carry variants in this way, and the answer now follows the same marking. |
| A mark is checked against the declared variants | Shared schema | `approved` | The reference check tested a mark against every symbol of the record, so a mark naming an index set or a law resolved. A mark, however, is not one symbol among the others: it names a variant. A mark naming no variant claims to hold of a nonexistent part of the class, and it also exempts its entry from the default rule: an unmarked declaration holds of every member. The check now reads the whole record, so a block that gains a mark stays covered. |
| The overreaching rule covers every block that refers to the class | Shared schema | `approved` | The rule read the laws alone, and it is not a rule about laws: an entry reaches the class through any key naming a symbol. In both problems with a partitioned class, all seven relations of the organization asserted of every member a coupling that only one variant can bear. The microbial environment that supplies resources also shares a vector that a member with direct interactions does not have. `overreaching_laws` became `overreaching_declarations`, over the laws, the organization and the external systems. |
| The unborne count reads every block that declares symbols | Shared schema | `approved` | The count enumerated five blocks by hand, although the record declares symbols in eight. A variant carried only by a relation or an external system would therefore have been reported as having no schema. The count now reads every block in which the record kind declares symbols. |
| Type variables in a signature | Shared schema | `approved` | An operator is general where the quantities that it acts on are particular, but the language could only name concrete sets. Ten of thirty-two local operators therefore wrote `open` for two different things. The rest were typed at their single call site, for example an adjoint over one problem's state space and a Jacobian over another's populations. `for_any` declares the variables that a signature may name, and four of sixteen still write `open`, each for a type that remains unknown. |
| Operators that the framework requires elevated | Shared schema | `approved` | Three gaps required canonical operators. First, no operator related two value sets, although such sets were first-class objects. Second, the domain carried a limit that no operator could take. Third, the `distribution` law role had no canonical statement, so three records wrote it two ways. `intersect`, `union`, `empty`, `complex`, `limit`, `norm`, `drawn_from` and `abs` join the canonical set. |
| Operators that no problem owns elevated | Shared schema | `approved` | `dot`, `adjoint`, `re`, `spectrum`, `jacobian`, `ft`, `grad`, `div` and `lap` are general mathematics. Yet each was declared and typed afresh by the problem that needed it, and `grad` received two incompatible types. Each is now typed once with its variables. |
| Vectors and matrices named | Shared schema | `approved` | `pow(reals, 3)` and `maps(prod(pop, pop), reals)` state a vector and a matrix without naming either. `pow` became `vectors`, and `matrices(S, A, B)` names the shape whose spelling a reader met eleven times. |
| A call checked against its signature | Shared schema | `approved` | No check compared a call with the arguments that its operator takes, so a signature narrower than necessary went undetected and a wrong arity passed. The check reads the registry and the problem's own operators, and covers ten calls in the turbulence target alone. |
| `d_t` replaced by `diff` | Shared schema | `approved` | The name hardcoded a course called `t`, and the record on delayed generalization differentiated along `step` while writing `d_t`. Neither alternative was free as a keyword: `d` is a quantity in two problems, and `rate` in a third. `diff` is free, multi-letter like the rest of the vocabulary, and the spelling used by the candidate surface of the syntax decision. Twelve calls to `diff` now name their index, the local second derivative is written as `diff` applied twice, and the arity check reads the two arguments. |
| The microbial course declared | Local case | `approved` | The record differentiated abundances along a course that it never declared and indexed them by none. The record now declares time as its course and indexes the abundance and the concentration by time. |
| Two `level` fields given distinct names | Shared schema | `approved` | One record carried two fields named `level`: `task.level` bounds the variables that an answer may use, and `task.required_accuracy.level` states how close the match must be. The registry exposed the collision: its key `task.level` named the second while the first's own subfields were keyed `task.compositional` and `task.substrate`. The block is now named `admissible_explanans` for the explanans that it bounds. The accuracy field is now `standard`, for the demand that it states. The note, its aliases, the satisfaction test and every inbound link follow the rename. |
| An explanans names the external systems that it appeals to | Shared schema | `approved` | The inclusion test admits an answer that explains by the structure of the inputs or by the class of system reading the outputs. No field could record that appeal, so the licence existed only in prose. `explanans[].appeals_to` names the external systems invoked, and their symbols became declarations so that an appeal resolves. [The vocabulary check](../src/gnomon/vocabulary.py) reports an appeal naming anything else. A quantity that the system owns is reached as a term under its own symbol, so the two routes never overlap. |
| An external system names its class | Shared schema | `approved` | The target is fixed by a reference class that its specifications generate. An external system, by contrast, had only a name in words and no typed field. `class` states the systems that it may be, so an appeal reaches a kind. Of the six external systems, one is fillable from declared symbols alone. The other five state beside the field why no class is fixed, and one of those would restate the quantity that it supplies. |
| A constraint carries the keys of a law | Shared schema | `approved` | Each constraint was a bare expression in a list. Each now carries a name, the places that its claim ranges over, its role and a gloss. The role is drawn from the law vocabulary, and constraints carry no separate one: two of the three are `state-constraint` and one `admissibility`. |
| The answer declares a model | Shared schema | `approved` | `terms` separated quantity, structure and property, although the value sets already carried that distinction. The answer also had no index sets, although its quantities took signatures. The answer now declares the blocks of a target system: `constituents`, `index_sets`, `variables`, `parameters` and `laws`. It references the entries that the target already holds. |
| A law states its warrant | Shared schema | `approved` | Only the answer carried a warrant, although a law of a target is equally admitted on a derivation, a posit, a fit or an instrument. Every claim resting on such a law inherits its exposure. Thirty laws now state why they are admitted: twenty-six posited by the model class, three derived and one measured. |
| The explanans is not only contrastive | Shared schema | `approved` | The explanans was defined as the difference between fact and foil. Three of seven questions state a contrast whose locus is `none`, so their explanans could not be filled. The claim now states the content that the answer relies on. The foils that a factor rules out are drawn from the list in the question. Each foil therefore had to carry a symbol that a factor can reference. |
| A law carries a symbol | Shared schema | `approved` | No field could refer to a law, so the organization of a mechanism could not be stated. The relations of that organization run between components and also between activities. In the framework, an activity is written as a law. Thirty laws are now declarations, unique within their record. |
| Organization replaces the influence | Shared schema | `approved` | `influence` belonged to a law and named the tuples along which its claim ran. Those tuples form one relation among components. An organization is a family of such relations. The relations now form their own block. Each relation carries a name and lists the tuples that it connects and its realizing laws. Its carrier is read from those laws. Seven relations span three problems, and one of them relates triples. |
| A factor states its chain | Shared schema | `approved` | The explanans recorded the elements that an answer relies on and not the work of each, so one quantity could be decisive in two accounts for unstated reasons. A factor now states its assertion, the element that it bears on, the laws and relations carrying its effect, and the result that it accounts for. |
| `gloss` split into five keys | Shared schema | `approved` | One key served five functions, and the twenty-six placeholders already distinguished the five. `reading` translates a formal field, and `justification` gives the reason for a choice. `differentiation` names the content that an entry adds to its neighbours, `procedure` describes how a step is performed, and `caveat` lists the gaps that the record leaves open. Of the 151 prose fields, 108 are readings, 30 justifications, 7 procedures and 6 differentiations. Three entries carried two functions in one string. Once separated, they yielded the three caveats. |
| The fourth kind of factor named for its block | Shared schema | `approved` | The three other values were nouns, and `organizational` was an adjective. The alternative `arrangement` would have named no block, although each other value names one. `organization` names the block that the factor bears on. `law-property` likewise points to the laws. |
| A factor is a property, possibly a found one | Shared schema | `approved` | The explanatory factor is rarely a declared element itself and usually a property of one. No record names the property before an analysis identifies the factor. The answer declares the property in its own model. A property of a law becomes one of the answer's laws, and a property of a structure becomes a relation of the answer's organization. The factor bears on the carrier of the property, possibly a family of elements. Where the property is found and not yet formalized, `holds` is open beside a caveat. |
| The satisfaction criteria realigned | Shared schema | `approved` | Two criteria named retired fields. The grain test asked for a *value scale*. That field was removed once the declared set of values was shown to fix which comparisons are meaningful. Separately, an example named the test `level`. Three criteria now name the fields that they compare: contrast with `separates`, relation with the chain that a factor names, and selection with a class conditioned on an outcome. Selection names that class because selection bias arises exactly when a class is conditioned on an outcome. |
| A candidate is held to its warrants | Shared schema | `approved` | Laws carried an untested warrant on both layers. `law-warrant` joins the warrant tier with three requirements. A law marked derived follows from its premises. A fitted or measured law names its data. A conclusion inherits the weakest warrant among the laws that it uses. |
| Option cells of the notes checked against the registry | Shared schema | `pending` | Still blocked: a note cell carries a gloss and the template writes a token, so `class of actual systems` and `class-actual` compare only once the note cells carry both. |
| The five choices local to the cases | Local case | `pending` | Unchanged. Each requires a decision from the designer. |

Two candidates *shrank* without changing owner. First, the requested implication of the microbial case is a `relation` manifestation, so it belongs to the phenomenon itself, before any epistemic task is added. The task candidate therefore retains only heterogeneous accuracy. Second, a scope can now refer to declared objects, so the candidate on scoped virtues retains only the binding between a virtue and the conclusion that it qualifies.

Unknown scientific results are not implementation candidates: their values remain `open`.

## Source ownership

Each responsibility has one source. Read the current owner before editing any dependent file. ^source-ownership

- [The syntax of symbols, expressions and signatures](../docs/epistemology/symbols-and-expressions.md) owns the language contract.
- [The meaning and admissible options of every inquiry field](../docs/epistemology/_index.md) own the semantic contract.
- [The machine-readable record shapes and their field order](../src/gnomon/data/templates/_index.md) own the representation contract.
- [The evidence and smallest required capability behind each reported problem](framework-issues-synthesis.md) owns the diagnosis, supported by the five case logs.
- [The alignment of method tables with templates](../tests/test_gnomon/test_template_alignment.py) and [the resolution of every symbol and reference](../tests/test_gnomon/test_symbol_resolution.py) own the structural checks.

## Verification contract

Each stage uses the narrowest checks that exercise its changes. A change to a shared template still requires checking every dependent fixture and downstream template.

- **Alignment of notes and templates:** each row of a method table resolves to one key, in the same order, and every key is documented.
- **Symbol resolution:** each symbol is unique within its record. References and the identifiers in a formula all resolve to declarations. Each rule has a planted negative case that fails for the intended reason.
- **YAML integrity:** every template and fixture parses, retains its `id` and `type`, and contains no uppercase placeholder.
- **Reference integrity:** each identifier that points to another record and each Markdown link resolves to one existing target.
- **Downstream consistency:** a change to a task requirement also updates every dependent answer or assessment field.

The documented `gnomon` conda environment is absent. Because Python must not run in the base environment, the read-only checks to date ran in `dev`. Obtain approval before creating the project environment or installing any package.

## Active constraints

- The designer controls each approval gate. Prepare a proposal and a targeted diff, but do not begin an unreviewed stage.
- Unknown scientific conclusions remain `open`. A protocol choice not yet made stays pending and never becomes `open`.
- Every stage changes its semantic source together with all dependent implementation and verification artifacts.
- The YAML templates remain pure data records. Explanatory prose belongs in the method notes or case descriptions.
- Every new Markdown file needs frontmatter and an entry in its folder index. Every renamed identifier requires sweeping all its inbound references.
- Reread each current file immediately before editing it and preserve concurrent user changes.
- The worktree contains unrelated changes under `.obsidian/` and `docs/methods-taxonomies/`. Do not modify or discard those changes.
- Changes under `docs/design/` require an explicit dependency and designer approval.
- No new package may be installed without approval.

## Open questions

| Decision | Why it matters | Blocked stage |
| --- | --- | --- |
| Which of the remaining candidates are approved? | The review states define the authorized scope. | Remaining candidates |
| Which concrete values resolve the five choices local to the cases? | Without those values, a fixture cannot distinguish an author choice from a scientific unknown. | Local repair |
| Does `scope_over_instances` nest inside each manifestation? | `scope_over_instances` qualifies a claim, and in the current schema a manifestation is the unit of assertion. The case on delayed generalization now forces the question. In that case, the ordering holds for every run, whereas the delay is an ensemble claim over initialization draws. One scope for the whole record therefore cannot qualify both claims. A gloss holds the distinction until the nesting is ratified. | Phenomenon schema |
| Should the expression grammar be enforced by a parser? | The current check is lexical: it resolves identifiers without parsing binders. Signatures are therefore declared by authors and not derived. | Stage B completion |
| Which schema technology is authorized? | The repository declares a schema directory but has no validator for inquiry records. | Provenance and validation |
| Which two candidate answers exercise the final trial? | The trial needs candidates that exercise prediction and proof obligations and that cited sources support. | Trial of the answer layer |

## Decision log

| Date | Decision | Rationale |
| --- | --- | --- |
| 2026-09-21 | Promote a limitation to the shared schema only when a typed distinction recurs or changes the admissible answers. | The rule keeps a complication specific to one case from expanding every record. |
| 2026-09-21 | Keep local author choices separate from unresolved scientific results. | Only scientific results warrant `open`. |
| 2026-09-22 | Reorient the records toward a modeling language, through [the two ratified stages](#^stages). | The records already depended on references, yet those links were unreliable, because a prose name carried a symbol, a gloss, a signature and sometimes a definition at once. |
| 2026-09-22 | Fold symbols into the fixture migration. | Symbols added after the migration would have required a further pass over every record. |
| 2026-09-22 | Derive grain from the signature and the definition. Retire the retention field, since it restated grain. | Every index absent from the signature was consumed by a binder that already names the map. |
| 2026-09-22 | Collect no table of record symbols, and keep the operator vocabulary in one source instead. | Each record scopes its own symbols, so their availability across the corpus needs no decision. The operator vocabulary, by contrast, needed one source, because of all vocabularies only the operator set was written in two places. |
| 2026-09-22 | Generate one symbol table per problem, reversing the earlier decision to collect none. | The earlier decision inferred that no table was needed because symbols have no scope across the corpus. An author fills and consults the records of one problem together, so a table serves at that level. |
| 2026-09-22 | Split the operator vocabulary by writer, per [the division by author and reach](#^three-vocabularies). | Of 26 operators in the canonical set, four were used by three or more problems and eight by none, since the set had grown one case at a time. |
| 2026-09-22 | Name a law's third role `constraint` rather than `coexistence`. | The records already labelled those laws an incompressibility constraint and an invariance of the state space, so the working term matched that usage. |
| 2026-09-23 | Keep the infix surface and define it over an S-expression core, per [the syntax decision](../docs/adr/expression-syntax.md). | A parser becomes available while records keep their current surface, and an operator stays a declared symbol, never a reserved word. |
| 2026-09-23 | Write a LaTeX form in single quotes. | A serializer reads the backslash of `\nu` inside double quotes as an escape, so the form arrives as a newline and a letter. The defect is of the same class as the symbol that a serializer read as a boolean. |
| 2026-09-23 | Write a deliberately invalid value as `no-such-role`, a string that no single edit turns into an admitted token. | A string close to an admitted value reads as a typo awaiting correction. Once corrected, such a string leaves the check unable to fail. |
| 2026-09-22 | Treat `status` as one concept whose states vary with the record kind, not as a generic slot. | Where `kind` named seven unrelated distinctions, `status` names the lifecycle of a record everywhere it appears. Only the admissible states depend on the content of the record. |
| 2026-09-22 | Declare every vocabulary and requirement in package data, and let the notes document those registries. | A checker that parses prose couples the logic of the framework to the formatting of a document. Such a checker also made the package depend on a workspace that holds `docs/`. Only the alignment check has reason to read a document. |
| 2026-09-22 | Record no list, per aim, of fields that must stay empty. | Three counterexamples refute such a list: a formal model class appears with two matched outputs, with one, and with none. Neither the aim, the target type nor the requested relation therefore decides the exclusion. |
| 2026-09-22 | Admit a gloss beside an expression, bounded by the rule that it carries no commitment. | The language became formal enough that a reader must reconstruct a condition such as `d_t(N) = 0`. A gloss in words costs less than that reconstruction or a guess. |
| 2026-09-22 | Declare a quantity of an external system among the record's own entries, and mark it with its owner. | The uniqueness rule, the resolver and every inbound reference assume one flat symbol table per record. A quantity nested inside an external system would introduce scopes for one attribute. |
| 2026-09-22, revised 2026-09-24 | Keep the constraints of an external system in its entry, outside the laws, and give each one the form of a law. | Two of the three reasons for keeping the constraints bare have expired. `granularity` is retired, and a role applies: of the three constraints written, two are `state-constraint` and one is an admissibility. The third reason, the object that a claim governs, was contestable from the start, and a constraint ranges over places as a law does. The constraints nevertheless stay with the external systems, because their block exists to state the boundary and no law of the target concerns an external system. |
| 2026-09-22 | Infer a shared variable's direction from its declaring block, and retire the field that stated it separately. | Input, output and coupling restated whether the variable is exogenous or internal, so a check was needed to hold the two consistent. |
| 2026-09-22 | Declare a variable set outside the system that varies during the dynamics as exogenous. | The rule that an interface variable is a state variable or a parameter forced a false choice. A driving force, a prescribed trajectory and a noise process are determined outside the system, yet none is held fixed. |
| 2026-09-22 | Type operators with `signature` and `values` rather than `takes` and `gives`. | A quantity indexed by a set and an operator taking an argument in that set have the same shape. Moreover, quantities and the method notes already relied on the two keywords. |
| 2026-09-22 | Move the overparameterization restriction from the question's domain to the selection criteria of the reference class. | Membership depends on a joint property of the architecture and the sample that no other specification fixes. A selection criterion records exactly such a property. Moreover, all five problems had left the field empty, while one of them carried its criterion as prose elsewhere. |
| 2026-09-22 | Carry every formula under `expression`, and let its block fix its role. | A law can be false and a definition cannot, but the block already separates the two, so two field names encoded the distinction twice. |
| 2026-09-23 | Open every entry with its name and its statement, before its symbol and its notation. | A reader identifies an entry by its name, and the symbol serves afterwards as the handle for the checker and for other expressions. |
| 2026-09-23 | Remove a definition that restates a law that the same record states. | A law can be false of the system and a definition cannot, so one formula carried under both hid which commitment the record makes. |
| 2026-09-23 | Divide the three identifiers of an entry. The name labels the entry, formulas use the symbol as a handle, and the gloss states the content that neither of them carries. | A gloss that paraphrases its name reads as content and states nothing, so it survives review while the entry stays unexplained. |
| 2026-09-23 | Collect the operators of a problem in one file, read against all its records. | An operator was typed over sets declared in the target and used in the phenomenon, so neither record could both own it and hold every set that its type names. |
| 2026-09-23 | Carry the reason for a requirement in a gloss beside it, and no longer in the case description. | Each restriction states the cases that it excludes. No field stated the reason, so a later reader could not tell a scientific commitment from an arbitrary narrowing. |
| 2026-09-23 | Retire `task.level.aggregation` without a replacement. | Every proposed replacement carried a presupposition that the level must not make: that an explanans uses aggregates, and that each of them relates a single variable to another. A satisfaction test already checks the admissibility of an aggregate. [The declaration that introduces the aggregate](../docs/epistemology/target-system.md#^aggregate-admissibility) carries the commitment. |
| 2026-09-23 | Do not infer from a case missing among the seven fixtures that no requirement exists. | The fixtures request no causal relation, yet inquiries seeking one are ordinary. A field is therefore justified by the *design* of the framework. |
| 2026-09-23 | Name the record kinds that the checkers read, separately from the blocks that declare symbols. | One constant had served both purposes, so adding a kind that declares no symbol silently excluded it from every check. |
| 2026-09-23 | Keep a dead declaration wherever its purpose can be stated, and connect it to the record. | In three cases, the purpose could be stated. Four microbial coefficients now type the interaction specification. In the case on delayed generalization, a law now draws the parameters from the declared initialization distribution. In the case on active matter, a domain is now fixed by its size and its boundary geometry. Without these declarations, the typed content would be lost and the missing law hidden. |
| 2026-09-23 | Let a requirement path traverse a list, where it formerly read a block of tokens. | A field whose entries carry a reason is a list of mappings. An aim that requires a value in every entry therefore needs a path that traverses the list. |
| 2026-09-23 | Write a modulating condition as a relation over declared symbols, no longer as a direction in words. | `increasing` and `decreasing` were already canonical, since a manifestation may take the form of a dependence. The direction of a modulation therefore needed no vocabulary of its own. |
| 2026-09-23 | Admit no `open` in an external constraint, and mark a genuine unknown with `maturity` instead. | The value `open` carried two meanings that a reader cannot tell apart. One of the two meanings silently hides the quantities that a constraint would have named. |
| 2026-09-23 | Require every carrier of an interaction to be indexed by both its endpoints. | With this requirement, the record reads as a graph. A quantity indexed by the source alone is excluded, because it states a property of that endpoint and carries no influence on another part. |
| 2026-09-23 | Generate the interaction graph over kinds, not over members. | The ordered pairs that are joined are fixed by a parameter of a member model. A target record leaves that parameter unvalued, so it states where to read the edges and lists no edge. |
| 2026-09-23 | Express a variant as a partition of one symbol table, not as a nested schema. | The uniqueness rule and every inbound reference assume one flat table per record, so a nested schema would introduce scopes for one attribute. |
| 2026-09-23 | Keep at the root a quantity that every member has, and let the laws differ. | A growth law renamed per variant broke the microbial phenomenon that takes its Jacobian. Every member has a growth law, and these laws differ only in their arguments. The law itself states those arguments. |
| 2026-09-23 | State an interaction on the law that asserts it, not in a block of its own. | Wherever a law existed, the block restated that law. Where none existed, the record could write one: a dependence of a growth law on a coefficient is falsifiable and expressible with the canonical relations. |
| 2026-09-23 | Merge the internal and exogenous variables, and keep the parameters apart. | No key belonged to one of the three blocks alone, so the first two encoded one distinction. The boundary of the third block survives because it carries a rule: a parameter may not be indexed by the course. |
| 2026-09-23 | Return the overparameterization restriction to the question, reversing the move of 2026-09-22. | The domain had held one parameter to a set, a form that no joint proposition fits, and that limitation forced the earlier move. The domain now carries a joint restriction, so the constraint sits with the inquiry that imposes it and another question may be put below the same line. |
| 2026-09-23 | Read the endpoints of an influence from the order of its pairs. | Separate names for the endpoints restated `applies_to` on one side and the pair order on the other. For an influence between two parts of one kind, the pair of names carried no information. |
| 2026-09-23 | Rebuild a vocabulary from the assertions of the records, not from the needs of the first cases. | `constraint` had absorbed three fifths of the laws, as `distribution-statistics` had the aggregations. A value that draws no distinction hides the claims of the entries that it labels. |
| 2026-09-23 | Admit an influence of more than two places. | A binary product presupposed that every influence has one source. The phenomenon records were corrected for exactly that fixed arity: a higher-order term has an effect that neither of its two sources carries alone. |
| 2026-09-23 | Merge two blocks when their boundary only restates the quantity that an entry names. | Restrictions on a parameter and on a variable differ in the kind of quantity that they name. The target record already classifies that kind. The separate blocks left implicit and unchecked the only rule that the boundary carried: only a variable can be clamped or held stationary. |
| 2026-09-23 | Read which members carry a symbol from the target, in the phenomenon as in the laws. | A phenomenon declares no variant of its own. The rule that an unmarked claim must be readable by every member that it ranges over therefore needs a fact held one record upstream, in the target. |
| 2026-09-23 | Let a maturity marker license a gap that a check would otherwise report. | The marker had no consumer, so a record could admit a gap and no check would read the admission. A check that consults the marker distinguishes an omission from a decision. |
| 2026-09-23 | Give an operator type variables before elevating any. | An operator typed at its one call site carries that site into every inheriting record. An elevated adjoint would thus have imported the state space of one problem into all those records. |
| 2026-09-23 | Replace `pow` with a vector constructor, and keep no second spelling for the same shape. | Every use of `pow` in the corpus was a vector, so keeping both would have left two spellings for one shape. |
| 2026-09-23 | Name the derivative `diff` rather than `d`. | Operators and quantities share one namespace, and two problems declare `d` as a dimension. A canonical `d` would therefore leave `d(u, t)` ambiguous to a reader and to the parser that the syntax decision anticipates. |
| 2026-09-23 | Name a field after the distinction that it decides. Within one record, give no two fields the same name. | `level` suggested a position in a hierarchy while bounding a vocabulary, and a reader of the registry could not tell which of the two fields its key named. |
| 2026-09-24 | Let an explanans appeal to an external system, and not to one of its quantities. | An owned quantity is already reachable as a term under its own symbol, so admitting both would write one appeal two ways. |
| 2026-09-24 | Refine a constraint by the vocabulary that laws *already* use. | The three constraints written fall inside the roles rebuilt from the 29 laws, so a second vocabulary would have named the same distinctions twice. |
| 2026-09-24 | Give a law of the target the warrant that its counterpart in the answer layer already carried. | Every law of a target is admitted, but not on equal grounds. A law derived from other laws stands or falls with its premises. A law posited, fitted or measured rests on a choice, a fit or an instrument, and new evidence revises it first. Every result resting on such a law inherits that exposure, up to the answer. The laws on which a result depends can therefore be read only once each law states why it is admitted. Of the 30 laws written, 26 are posited by the model class, 3 are derived and 1 is measured. |
| 2026-09-24 | Declare the answer's model with the target's blocks, not with a vocabulary of its own. | A candidate answer proposes a model of the target system. A separate vocabulary for that model drew its own distinctions. Those distinctions duplicated the divisions that the value sets already drew. |
| 2026-09-24 | Make the organization one block, not a key on each law. | In the literature on mechanisms, organization is a family of typed relations over components and activities. A key on a law can carry only the single relation that its law states. One block holds the family, and a relation realized by several laws takes one entry. |
| 2026-09-24 | Name the block `organization` and its key `relates`, retiring `influence`. | `influence` named the causal reading of one relation. The block holds relations that need not be causal: adjacency and sameness are organizational, and neither influences another part. |
| 2026-09-24 | Split the prose field by the job that it does, since renaming one key does not suffice. | The word `gloss` suggested a superficial remark, yet the field holds mandatory content. No single word covered its five jobs. For example, of all prose values, 108 render an expression in words, whereas another 35 sit beside no formal field and therefore have no expression to translate. |
| 2026-09-24 | Keep the vocabulary of factors, and let each bear on several elements. | The earlier reading that the vocabulary restates `bears_on` was mistaken. A `law-property` is a property of a law, such as invariance, additivity or factorization. No record names a law property before an answer uncovers one. Each kind names the object that carries the factor, and a property of a family bears on each of its members. |
| 2026-09-24 | Check the warrant that a law claims, beyond recording it in a field. | The decision that a warrant propagates to every result resting on it has no force unless a check confirms that each law marked derived follows from its premises. Without that check, the field would record only an intention. |
| 2026-09-25 | Rename the opaque option values and the reference keys of an assessment. | Each value now names what it selects: `class-actual` and `class-possible` became `actual-systems` and `possible-systems`, `parameter-draw` and `structure-draw` became `random-parameters` and `random-structure`, `link` became `mapping`, and `function` became `functional`, the only noun among the adjectives of the requested relation. The keys `assesses` and `against` became `answer` and `question`, like the reference keys of the other records. |
| 2026-09-25 | Never list `open` in a closed vocabulary, and name the first state of a question `posed`. | The registry accepts `open` in every closed field and stated that it lists none, yet twelve vocabularies listed it. The lifecycle state `open` of a question also gave the sentinel a second meaning, so that state is now `posed`. |

## Synchronization debt

- `realizations` remains unexercised. The candidate answer on grokking, by contrast, now uses `among`, but that use is unchecked, because the validation pipeline excludes answer records.
- `preferred` is now exercised by the grokking question and assessment. The candidate fails the filtering tiers, and that failure exposes a missing structured state: appraisal withheld, as distinct from a ranked answer supplied.
- A constitutive virtue is settled by the product test and a preferred virtue by appraisal, each exactly once. Filled assessments exercise this rule, and no check enforces it yet.

- The signature of a derived observable is declared, not computed. Until a parser derives it, a record can declare a signature that its definition contradicts. The resolution test checks references and leaves arity unchecked.
- The canonical operator set is closed by no grammar. [The operator groups](../docs/epistemology/symbols-and-expressions.md#^operator-groups) list the set, and a problem needing more operators declares them in its own `operators.yml`.
- The package is not installed in any environment, so the test suite reaches it through `pythonpath` and the command through `PYTHONPATH=src`. An installed `gnomon vocabulary` script requires an authorized project environment.
- The Markdown view of a vocabulary is built, but the view of a filled inquiry record is not, and neither typesets an expression as LaTeX. The inputs of both views, however, exist: the records carry the syntax already, and the registry and the `operators.yml` of each problem already hold the notation that the renderer needs for each operator.
- Of the six constraints written for external systems, half are expressions. Each of the other three is empty, with an adjacent statement of why the system imposes no constraint. For the measurement system, coverage and uncertainty bear on the detection criterion and not on the target, so no record declares the two quantities. A detection test that used them would declare them among its inputs.
- `status` carries a different vocabulary in each record kind. Unlike `kind`, `status` names one concept: the lifecycle of a record, whose admissible states depend on its content. Its repetition across kinds is therefore admitted, with no generic slot in its place.
- Only the rows reading `always` are checked, because the other conditions in the same column are semantic, such as an index set required when no constituent kind or parameter supplies the set.
- A tolerance separates the metric from the bound, so an inquiry can state how it compares candidates without stating how close they must come. In the fixtures, one case specifies a metric, and another fixes neither. A qualitative accuracy carries no tolerance at all.
- A renderer that composes the question statement from the fields remains to be built, together with a LaTeX typesetter for a filled record. [The choice of an infix surface over an S-expression core](../docs/adr/expression-syntax.md) fixes the input notation of such a renderer.
- All six detection tests read `expression: open`, since each would test the pattern that its inquiry seeks. Until the diagnostic exists, the glosses of these tests state the check that each test performs.
- The key order within an entry is stated in [the representation contract](../src/gnomon/data/templates/_index.md) and enforced by no check. A record whose keys run in another order passes every test.
- The lattice realization in the case on active matter is named, and its schema is not written. The check now reports such a variant unless the record marks it open. This record marks the variant open, so the gap is declared, not closed. No lattice member can be generated, and the `continuum` marks on the target and the phenomenon carry information only because a second variant is named.
- The optimizer update of the record on delayed generalization states how the parameters change and not how its auxiliary state does. The former call `d_t(theta, z)` passed two quantities to a derivative, a call that no reading admits. The call now reads `diff(theta, step)`, and the law for `z` remains unwritten.
- `grad`, `div` and `lap` are typed with a free variable for the dimension, and the type language cannot bind it to the domain over which the operators run. The types state the shape correctly and leave the dimension unconstrained.
- The arity of a relation of the organization is stated and only partly checked, since the check requires only that every place index the carrier. A relation may therefore name fewer places than its carrier has and still pass every check. A relation that names a place outside every carrier is reported.
- The direction of a relation of the organization is stated and cannot be checked. An edge with its pairs reversed passes every check, since both endpoints index the carrier either way. The defect belongs to the same class as the declared signature, but lies beyond the reach of a parser.
- The limit once recorded for `applies_to` predates the retirement of that key and of the interaction laws. Whether the `signature` check shares its blind spot, a quantity applied at a literal index, remains to be verified.
- The command is `python -m gnomon`, never `python -m gnomon.cli`: the latter has no `__main__` guard, so it imports the module, exits 0 and performs no action. Invoked that way, a regeneration reports success and writes nothing.
- The Earth-system and microbial answers now use appeals by an explanans, but the checker still reads that record kind only through synthetic unit fixtures.
- Six answer and assessment pairs now exercise the rewritten templates. [Their shared validation and representation gaps](answer-assessment-template-issues.md) supersede the former absence of instances.
- A detection test carries a name and no symbol, since no field references a test. A symbol becomes warranted as soon as an assessment reports a verdict per test.
- The rule that a gloss must not reformulate its name is checked by hand. The supporting check is a heuristic counting shared words, so it reports entries that a reader must triage, and for that reason it is not in the suite.
- A gloss is authored prose beside a formula, and no check ties the two together. A gloss that contradicts its expression passes every check. Only the style keys and a reader detect such a conflict.
- Unused quantities are detected lexically, as undeclared operators are, so a symbol named only inside a gloss counts as reached. A structural count would read the expression and reference fields alone.
- A domain limit is one string pairing a quantity with a value, `Re -> infinity`. Both ends resolve, and the arrow between them is punctuation that no operator defines, so no check verifies that the left side is a declared quantity of the target.
- The corpus fills three precipitating conditions and two modulating ones, but no inhibiting one, no nonstandard circumstance and no byproduct. Each block left empty carries a comment giving the cause specific to its case. The three kinds with no instance need quantities that no case declares: a dissipation rate for the turbulent anomaly, an intervention scale for a nonstandard preparation. The gap lies in the fixtures: the schema now expresses all five kinds.
- Nineteen value sets read `open` because the case fixes no set. Their identifying content sits in the gloss, the place where a name in words belongs. A set that is genuinely constrained and not yet expressible has no other home.
- The option cells of the notes remain unchecked against the registry, because a note states a gloss where a template holds a token. A cell carrying both the gloss and the token would close the last unchecked alignment.
- The schema index lists registry files that its directory lacks. Work on inquiry schemas must update the index without claiming that those unrelated registry files were restored.
- [The assessment record](../src/gnomon/data/templates/_index.md) carries no symbol and no expression. Filled answer records now exercise the expression language under manual inspection, but the loader still excludes them from automated resolution.
